---
interface Props {
  title: string;
  index?: string;
}

const { title, index } = Astro.props;
---

<h3 class="text-xl text-primary font-mono tracking-widest mb-6 uppercase flex items-center gap-2">
  {index && <span>{index} //</span>}
  {title}
</h3>
